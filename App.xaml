<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<!-- Copyright (C) 2026 SaveOver -->
<Application
    x:Class="SaveOver.Sheltered2.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SaveOver.Sheltered2">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>

            <!-- The TitleBar sits in its own row above the NavigationView rather than overlapping
                 it, so the content needs no top margin to clear a title bar. -->
            <Thickness x:Key="NavigationViewContentMargin">0</Thickness>
            <Thickness x:Key="NavigationViewMinimalContentMargin">0</Thickness>
            <Thickness x:Key="NavigationViewContentGridBorderThickness">1,1,0,0</Thickness>
            <CornerRadius x:Key="NavigationViewContentGridCornerRadius">8,0,0,0</CornerRadius>

            <!-- Keep page content readable on large windows without constraining the shell or
                 responsive page roots. Inventory uses the wider value for its dense card grid. -->
            <x:Double x:Key="PageContentMaxWidth">1100</x:Double>
            <x:Double x:Key="WidePageContentMaxWidth">1280</x:Double>

            <!-- A page section: the standard Fluent card. Every card in the app uses this, so the
                 editor pages and Home/Donate read as one surface. -->
            <Style x:Key="CardBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="{StaticResource OverlayCornerRadius}"/>
                <Setter Property="Padding" Value="24"/>
            </Style>

            <!-- A well inside a card, holding a value the user is meant to copy. -->
            <Style x:Key="InsetBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource LayerFillColorDefaultBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}"/>
                <Setter Property="Padding" Value="12,8"/>
            </Style>

            <!-- Verbatim text - a path or a wallet address - shown for copying. -->
            <Style x:Key="MonospaceTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Consolas"/>
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
                <Setter Property="IsTextSelectionEnabled" Value="True"/>
            </Style>

            <!-- Copy button carrying an icon -->
            <Style x:Key="IconButtonStyle" TargetType="Button">
                <Setter Property="Padding" Value="8"/>
                <Setter Property="CornerRadius" Value="4"/>
            </Style>

            <!-- The heading above a group of settings rows. -->
            <Style x:Key="SettingsSectionHeaderTextBlockStyle"
                   BasedOn="{StaticResource BodyStrongTextBlockStyle}"
                   TargetType="TextBlock">
                <Setter Property="Margin" Value="1,28,0,4"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
